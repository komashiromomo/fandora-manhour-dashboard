# Fandora 人工時管理系統 — 開發紀錄

> 從一個 Excel 檔到一套即時 BI dashboard 的歷程

> **簡報注意**：本文件記錄的「過程數字」（如開發天數、bug 修復數、commit 數）皆為事實。
> 但「資料層面數字」（工時筆數、IP 工時排行、估算成本等）是文件撰寫當下的快照，會隨
> Drive 上資料變動。**對 COO 簡報時請直接從 dashboard 即時截圖**，避免引用本文件中的
> 中間數字（特別是開發過程中曾因 parser bug 看到的污染數值，現已修正）。

---

## 一、為什麼要做這個系統

### 痛點

過去工時管理在 Google Sheets 上散落維護：

| 問題 | 影響 |
|---|---|
| 13 位員工各自一份「個人版工作日誌」+ 主管彙整版本 | 月底結算工時要人工 vlookup 比對 |
| IP × 部門 × 工作項目 × 員工 多維度，但 sheet 只能呈現 2D | 想看「老高與小茉這個 IP 在 2026 Q1 共投入多少工時 / 哪些人」沒辦法即時得到 |
| 工時無法直接對應到費用 | 每個專案的真實成本 = 直接成本 + 分攤管銷，但管銷要怎麼分？ |
| 員工填寫品質參差不齊 | 有人把 IP 名稱填到「工作項目」欄，造成統計失真，但 sheet 無法自動偵測 |

### 系統目標

- **真實工時可視化**：從原始日誌到 dashboard 全自動，免人工結算
- **多維度交叉分析**：IP × 部門 × 員工 × 工作項目 × 月份 任意組合下鑽
- **精算管銷分攤**：把公司每月管銷（人事費 + 房租 + 硬體 + 雜費）按工時佔比攤到每個 IP，得到「真實獲利前的成本」
- **資料品質守門員**：自動偵測員工誤填、低品質記錄並提醒

---

## 二、開發歷程（時間軸）

整體開發週期：**2026 / 3 / 25 – 2026 / 5 / 4（41 天）**，包含三次嘗試與一次接手。

```
3/25 ─── 3/31    第一次 MVP（單檔 611 行 + GitHub Pages）        7 天
4/4  ─── 4/9     第二次模組化嘗試（React + Vite 雛形）            6 天
4/13 ─── 4/24    第三次重寫（clean slate，做完 v3 Phase 1-3）    12 天
4/26 ─── 5/4     接手 v3 後的修復 + 擴充                          9 天
─────────────────────────────────────────────────────────
                                                           合計 ≈ 41 天
```

### 階段零：第一次 MVP — 單檔暴力解法（3 / 25 – 3 / 31，7 天）

做了一個 611 行的單檔 React 應用，部署在 GitHub Pages（`komashiromomo/fandora-hr-dashboard`），能跑但問題堆積：
- Excel 日期欄位（45662 這種 number）被誤判成專案名稱
- 同一員工有 3 種名字（中文 / 英文 / 暱稱），統計時對不上
- 接續列空白會丟資料（員工不會把每行都重複填名字）
- 用 API Key 認證無法存取個人 Drive

那段時間花了 3 小時搞清楚 Excel 結構、2 小時修日期解析。**這次的學習都濃縮成後來重寫的「不能踩」清單**。

### 階段一：第二次嘗試 — 模組化失敗（4 / 4 – 4 / 9，6 天）

把 611 行單檔拆成 34 個模組化檔案，但過程中：
- 架構方向反覆切換 3+ 次（CDN ↔ Vite）
- 34 個檔案零 git commit 直到部署
- 缺 client_id 導致 Vercel 上白屏 crash
- 新舊 index.html 混淆

**過程中完全沒人能看到 dashboard 跑出來**。最後決定砍掉重來。

教訓：先小、先穩、先 commit、先部署一個空白 app 確認 pipeline，再放邏輯進去。

### 階段二：第三次重寫 — 從零再來一次（4 / 13 – 4 / 24，12 天）

`chore: clean slate for guided rebuild` 開始第三次（v3）。這次有計畫地走：
- 切到新 repo `fandora-manhour-dashboard`、新部署 Vercel
- 第一步：先部署一個空白 app 到 Vercel，鎖定 Vite 不再切回 CDN
- 寫 types.js 定義所有介面合約（解決上次 setData 介面誤用造成的 bug）
- 每個里程碑 commit + push + Vercel 驗證
- Phase 1：基礎 Dashboard 完成（資料層、登入、6 個 page UI）
- Phase 2：授權 IP / 工作項目分類
- Phase 3：專案成本分攤（用員工月薪舊公式）
- 完成 Fandora 設計系統初步整合

到 4/24 完成 v3 的 Phase 1-3，但因為實際 Drive 連線、解析器接線等等仍有許多細節未完工。

### 階段三：接手 v3（4 / 26 – 5 / 4，9 天）

從第三次重寫的半成品接手，從這裡到正式可用之間還有不少坑要填。可分 6 個小階段：

---

#### 階段三-1：把資料管路打通（4 / 26 – 4 / 28）

> 「先讓老闆看得到任何一筆資料，再說設計。」

**面對的現實**

接手時發現 v3 是個「殼」：登入頁、頁籤、UI 都做了，但 **真正的資料解析器是空的**。從 Drive 載入按下去什麼都不會發生，但前端不會報錯（靜默失敗）。

**每一道關卡與克服**

| 卡關 | 解決方式 |
|---|---|
| 登入按鈕按了沒反應 | Google 帳號登入的 React 元件接線錯誤，修好按鈕能渲染 |
| 登入後看不到「設定」頁 | 角色判定機制沒接上，所有人都被當一般成員。改成 admin 白名單 + 公司權限表 |
| 同事帳號登入後中文名字讓畫面卡死 | Google 回傳的姓名含「捲毛」這種中文字，前端解碼程序對中文支援有 bug，修好 |
| Drive 上的工作日誌資料夾抓不到內容 | 公司用的是「共用雲端硬碟」（Shared Drive），Drive API 預設不會搜共用硬碟，要明確標示 |
| 載入後每個檔案產 0 筆資料 | Excel 日期欄是 number（45662 代表某日的 Excel 序號），程式把它當字串處理，整列被跳過 |
| 每次 reload 都要重新解析 21 個檔，一次 30 秒 | 加入瀏覽器本機快取，reload 後直接從快取還原；只有主動刷新才重抓 |

**里程碑**：成功從 Drive 自動拉 21 個個人版日誌（橫跨 9 個月），解析出全公司工時時間序列。

---

#### 階段三-2：套上 Fandora 設計風格（4 / 28）

接手時 dashboard 是預設藍色 + Ant Design 標準風格，跟公司品牌完全沒關係。

**做法**：用 Claude Design 工具一起跟著設計師（AI assistant）迭代了一輪「人工時系統 V2」設計：

- 採用公司 CI 配色（cyan #00A4C6 主色 + 橘 / 暖配色）
- 字型統一 Noto Sans TC + Roboto + Fredoka（標題用）
- **改成左側欄導覽**（取代頂端頁籤），分成「分析」「系統」兩組
- Header 加上麵包屑、即時同步指示、品牌標識
- 所有圖表配色換成設計 tokens 控制
- 6 個分頁全部重寫成設計風

**設計系統能力**：所有顏色、間距、半徑、陰影都用 CSS 變數管理，未來換主題色只要改 tokens。

---

#### 階段三-3：成本分攤從個人月薪改為公司月度管銷（5 / 4）

接手時的成本邏輯是「員工月薪 × 工時佔比 → 專案成本」，但 Fandora 實際提供的「Cost Sheet」不是員工薪資，而是**公司每月管銷費用表**：

| 月份 | 人事費 | 房租場租 | 硬體系統費用 | 雜費 | 總計 |
|---|---|---|---|---|---|
| 2026/04 | $1,000,000 | $100,000 | $40,000 | $60,000 | $1,200,000 |

**重新定義公式**：

```
專案 P 在月份 M 的成本 = 該月公司總管銷 × (P 在該月工時 / 全公司在該月總工時)
專案 P 的總成本 = Σ_所有月份 上述
```

部門、員工、工作項目同樣的算法。所以一個 IP 投入越多工時，分攤到的管銷越多 — 反映真實的資源排擠成本。

**這個公式帶來的能力**：

- 每個 IP 自動算出當期累計成本（依當月公司管銷與全公司工時動態調整）
- 同樣的工時若那個月公司管銷比較重（年終獎金月、新硬體採購月），分攤就比較重 — 反映季節性
- 全公司平均時薪（公司管銷 ÷ 總工時）作為基準，可比較哪些 IP 的「每小時產出成本」高於 / 低於平均

---

#### 階段三-4：多維度進階分析（5 / 4）

從 5 個分析頁（總覽 / 專案 / 部門 / 人員 / 工時類型）擴充到 **可任意層次下鑽**：

```
專案頁 ──點老高與小茉──▶ 抽屜開啟「老高與小茉」全面分析
         ──點工作項目「商開設計」──▶ 篩成「老高與小茉 × 商開設計」
         ──點員工「余凱紓」──▶ 再篩成「老高與小茉 × 商開設計 × 余凱紓」
         ──點 2026-04──▶ 看到該月每天的詳細記錄
```

5 個維度（IP / 員工 / 部門 / 工作項目 / 月份）可任意組合，**麵包屑**讓你逐層回退。

意義：經營層問「老高與小茉的成本主要是商開部花的，還是視覺設計部花的？」三秒內可以給答案。

---

> 註：簡報時「具體數字」請打開 dashboard 截目前畫面，不要引用本文件中的中間數字。
> 開發過程中曾因為 parser bug 一度看到「老高與小茉 4,323 小時」這種虛高值（範例列污染），
> 修好後是 69 小時，差距很大，請以 dashboard 即時顯示為準。

#### 階段三-5：資料品質守門員（5 / 3 – 5 / 4）

**問題**：員工填寫工時欄位很自由，常見三類錯誤：

1. **把 IP 名稱填到「工作項目」欄**：例如把「魔物獵人」當成今天做的事情記下來，但「魔物獵人」是 IP（作品名），不是工作（動作）
2. **範例列被當成資料**：日誌前 2 列是給員工示範填法的「範例 1 / 範例 2」，但程式以為是真的工時
3. **範例列的 IP 帶到後面**：範例 2 通常填「老高與小茉」當示意，造成所有沒填 IP 的列被誤算成「老高與小茉」（這個 bug 害我們以為老高佔了 4254 小時，實際只有 69）

**解法**：

- **嚴格日期驗證**：只有 A 欄填了實際日期（4/1、2026/04/01 或 Excel 序號）的列才算資料，header / 範例 / 空白全部跳過
- **IP 誤填偵測 + 警示**：六個分析頁頂部都會顯示橘色警示卡：「⚠ N 個工作項目其實是 IP 名稱，共 X 小時，N 位員工」，點擊可看到「誰寫錯」明細表
- **每週自動健檢**：每 7 天打開 dashboard 第一次，自動跳出 modal 點名本週仍在誤填的員工，可一鍵 snooze 一個月
- **可維護的 IP 清單**：設定頁加「IP 清單管理」，自動偵測 Drive 上實際出現的 IP，發現「不在清單上的新 IP」會列出來，點一下就加入；清單變動所有頁面即時生效

---

#### 階段三-6：分享、自動化、視覺風格（5 / 4）

**半自動同步**：dashboard 預設每 30 分鐘背景靜默拉一次 Drive 新資料，不打擾使用者；可在右下角設定面板調 15 / 30 / 60 / 240 分鐘或關閉。token 過期會靜默放棄這次（不會跳授權視窗），等使用者下次親自操作再處理。

**可分享連結（最新）**：

dashboard 改成 hash routing，URL 反映當前看到的 view：

```
fandora-manhour-dashboard.vercel.app/#/projects/老高與小茉
fandora-manhour-dashboard.vercel.app/#/projects/老高與小茉/workType:商開設計/employee:余凱紓
```

複製這個連結貼給同事，打開直接落到那個 view。Header 加了「複製連結」按鈕一鍵複製。

**動森島嶼風格**：跟設計工具迭代出第二套 UI 風格（牛皮紙厚邊框、3D 凸起陰影、圓潤 pill 按鈕、四季背景、飄落葉子動畫），可在右下角即時切換「簡潔 ↔ 動森」。同一份資料兩套外觀，氛圍會議用動森，給投資人看用簡潔。

---

## 三、目前的能力盤點

### 資料

- **13 位員工的個人工時時間序列**（2025 年 9 月 – 2026 年 5 月，9 個月）
- **27 份 Google Sheets** 自動拉取（21 份個人版 + 6 份每月彙整版，後者目前不解析）
- **23 個月份**的公司管銷費用（人事 / 房租 / 硬體 / 雜費）
- **30 個系統內建 IP** + 自訂 IP 清單可動態擴充
- *（精確的工時筆數、各 IP 排行請以 dashboard 即時顯示為準）*

### 五個分析角度

| 角度 | 看什麼 |
|---|---|
| **總覽** | 4 個 KPI（總工時 / 員工數 / 授權 IP 數 / 平均時薪）+ 月度走勢 + IP 占比 + 員工 × 月份熱力圖 |
| **專案分析** | 13 個 IP 的工時 / 成本 / 員工數 / 跨部門 排行；點任一 IP 進入完整分析 |
| **部門分析** | 7 個部門的工時 / 員工 / 估算成本；可深入個別部門 |
| **人員分析** | 13 位員工的工時 / 出勤 / 跨 IP / 估算個人成本 / 平均時薪 |
| **工時類型** | 全公司投入哪些動作（設計、開會、出貨…），自動排除 IP 誤填 |

### 自動化機制

- **背景同步**：每 30 分鐘自動從 Drive 拉新資料
- **每週健檢提醒**：誰填錯資料每週自動點名
- **快取與恢復**：reload 不重抓、token 自動刷新、舊 cache 偵測污染自動清

### 風格與設定

- **兩套設計風格**（簡潔 / 動森）即時切換
- **三個主色系**（冷 / 暖 / 深色）
- **三段排版密度**（寬鬆 / 平衡 / 密集）
- **權限欄位**（成本欄位僅 admin 可見）
- 所有設定持久化（瀏覽器記得你的偏好）

---

## 四、量化成果

| 指標 | 數字 |
|---|---|
**過程指標**（事實、不變）：

| 指標 | 數字 |
|---|---|
| 整體開發週期（從第一次 MVP 起算） | **41 天**（2026 / 3 / 25 – 5 / 4） |
| 三次嘗試 + 一次接手 | 第一次 MVP（7 天）→ 第二次模組化（6 天，未上線）→ 第三次重寫（12 天）→ 接手後修復擴充（9 天） |
| 主動開發日數（active development days） | 約 15 個工作日 |
| 程式碼版本數（接手後 commit） | 50+ 次有意義的迭代 |
| 接手後修復的明確 bug 數 | 12 個（從登入按鈕到日期解析） |
| 平均一次 Drive 載入耗時 | ~30 秒（21 個檔案） |
| 平均一次 reload 耗時 | < 1 秒（從快取） |

**資料涵蓋範圍**（會隨 Drive 同步增長）：

| 指標 | 數字 |
|---|---|
| 涵蓋的月份 | 2025 年 9 月 – 2026 年 5 月（9 個月） |
| 涵蓋的員工 | 13 人 |
| 涵蓋的部門 | 7 個 |
| 涵蓋的 IP 數 | 約 19 個（含非授權，新 IP 會自動偵測） |
| 工時筆數、各 IP 排行 | 以 dashboard 即時顯示為準（會變動） |

---

## 五、過程中關鍵的選擇

### 為什麼花了 41 天而不是直接買套裝？

實際 active 開發日數約 15 天，但**從第一次嘗試到上線之間經歷三次重寫**。原因：

1. **業務邏輯複雜度被低估** — 「員工有 3 種名字」「Excel 日期是 number」「接續列要 carry-forward」這些細節在開始前根本不知道，每個都要踩到才會浮現
2. **第二次失敗的代價** — 「34 檔案零 commit」「白屏 crash」這種事先看像小問題、實際上會卡住整個 demo
3. **第三次才用上前兩次的教訓** — Excel 解析的 5 個 critical bug、模組間介面合約、零配置防禦（沒 env 也能啟動），都是前兩次踩過才寫得對

**敘事重點**：每次失敗都不浪費，第三次能在 12 天做完 v3 主體，是因為前兩次累積了 13 天的「不能踩」清單。

### 為何不用市售 BI 工具（Looker / Power BI / Tableau）

- 工時源資料是 Google Sheets，欄位格式不統一（有人把 IP 填到工作項目欄、有人填中文有人填暱稱），先要做一層**清理 + 對應 + 容錯**才有意義
- 公司只想要「自己的 dashboard」不想再多一個 SaaS 帳號管理
- 成本分攤公式有特殊邏輯（「公司管銷 × 工時佔比」），需要客製化計算

自製成本：9 天工程，無持續訂閱費。

### 為何用 React SPA + Vercel

- 沒有後端 — 所有資料拉取與計算在瀏覽器內完成，安全（OAuth token 不離開瀏覽器）
- Vercel 自動部署，push 到 main 約 1 分鐘上線
- Google Drive API 直連，沒中間人

### 為何「兩種風格」設計

公司氛圍會議要的視覺感跟對外簡報要的不同。同一份資料、兩種人格，切換零成本。

---

## 六、下一步路線圖

### 已寫好規格、未開發

**專案損益系統**（規格書已撰寫）

接這個工時系統後，最後一塊拼圖是把 **營收 / 直接成本 / 授權成本 / 行銷費用** 也接進來。當前精算的「分攤管銷」就是損益表的成本端壓軸。預期介面：

```
營收           − 直接成本 = 毛利
毛利           − 授權成本 − 行銷費用 − 分攤管銷 = 營業淨利
營業淨利率     = 營業淨利 / 營收
工時投報       = 營業淨利 / 投入工時 (新指標)
```

每個 IP 一張月度損益走勢、預警「OP 為負」的 IP、跨期比較。

### 待強化

- **權限細分**：目前 admin / member 兩級；未來可加 finance role 只看損益、各部門主管只看自己部門
- **匯出 PDF / Excel**：給董事會月會用
- **手機版**：當前 1280px 以下會堆疊但體驗不夠順
- **資料品質指標卡**：目前只有 IP 誤填警示，可擴充「漏填日數」「異常時數（日 < 4h 或 > 12h）」「連續未填」等

---

## 七、重要連結

- **正式網址**：https://fandora-manhour-dashboard.vercel.app/
- **GitHub Repo**：https://github.com/komashiromomo/fandora-manhour-dashboard
- **設定頁**（Drive 連線、組織人員、IP 清單、診斷）：https://fandora-manhour-dashboard.vercel.app/#/settings

---

*文件版本：2026 / 5 / 4*
